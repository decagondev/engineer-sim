# Optional sketches

v1 is the design doc. If you want to make a trade-off concrete, drop it here:

- `capacity.md` — back-of-the-envelope (QPS × payload × cache hit rate)
- `api.yaml` — create / redirect / stats
- a tiny redirect handler

None of this is required to submit.

# CI/CD pipeline — starter

Ravi wants deploys "faster." Before optimising, talk to him about why deploys
feel slow and painful — the numbers may point somewhere else.

- `.ci/pipeline.yml` — the current pipeline (note the retry-on-failure step).
- `flaky_report.txt` — a rough dump of recent build failures.

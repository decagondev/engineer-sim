# Signup form — starter

Maya says the signup form is "broken" — people aren't signing up. Talk to her
before assuming what's wrong.

- `signup.html` — the form.
- `server.py` — a stub backend that handles submissions and sends a verify email.

Commit as you go.

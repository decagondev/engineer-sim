fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pyyaml>=6
pytest>=8
# Optional — only needed for LLM_PROVIDER=anthropic:
# anthropic>=0.39
# Optional — only needed for LLM_PROVIDER=groq:
# groq>=0.11

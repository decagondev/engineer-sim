fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pyyaml>=6
pytest>=8
# Optional — only needed for LLM_PROVIDER=anthropic:
# anthropic>=0.39
# Groq (LLM_PROVIDER=groq) uses httpx against the OpenAI-compatible API —
# no extra package. Set GROQ_API_KEY.

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
# In-process WebSockets: keep exactly one replica (DEPLOYMENT-PLAN.md T7).
numReplicas = 1
